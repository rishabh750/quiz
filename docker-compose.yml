services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - pgdata:/var/lib/postgresql/data
      - appdata:/data
    restart: unless-stopped

volumes:
  pgdata:
  appdata:
